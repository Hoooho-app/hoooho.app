import assert from'node:assert/strict';import test from'node:test';import type{HealthEventApiDto}from'../../types';import{buildVisitConcernCandidates}from'./visitConcernSelection'
const event=(id:string,title:string,start:string,status:HealthEventApiDto['status']='observing'):HealthEventApiDto=>({id,accountId:'a',memberId:'m',title,category:'other',status,startTime:start,createdAt:start,updatedAt:start})
test('同一连续病程的发热咳嗽记录合并为一个候选',()=>{const result=buildVisitConcernCandidates([event('1','发热','2026-09-09T10:00:00Z'),event('2','咳嗽加重','2026-09-11T10:00:00Z')],new Date('2026-09-13T10:00:00Z'));assert.equal(result.length,1);assert.equal(result[0].label,'发热伴咳嗽');assert.deepEqual(result[0].eventIds.sort(),['1','2'])})
test('明显独立的问题保持第二候选且已康复项目不进入',()=>{const result=buildVisitConcernCandidates([event('1','发热','2026-09-09T10:00:00Z'),event('2','膝部摔伤','2026-09-12T10:00:00Z'),event('3','旧咳嗽','2026-08-01T10:00:00Z','recovered')]);assert.equal(result.length,2);assert.ok(result.some(item=>item.label==='膝部摔伤'))})
