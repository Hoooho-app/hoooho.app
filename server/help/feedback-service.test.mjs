import assert from 'node:assert/strict'; import test from 'node:test'; import { FeedbackService } from './feedback-service.mjs'
test('feedback validates and persists a privacy-limited record',async()=>{let data={feedback:[]};const service=new FeedbackService({store:{update:async(fn)=>{data=fn(data)}}});const result=await service.create('account-1',{category:'故障排查',description:'帮助中心搜索后页面没有响应',page:'帮助中心',device:'手机',includeDiagnostics:true},new Date('2026-08-27T00:00:00Z'));assert.equal(result.status,'received');assert.equal(data.feedback.length,1);assert.equal(data.feedback[0].accountId,'account-1')})
test('feedback rejects short descriptions',async()=>{const service=new FeedbackService({store:{update:async()=>{}}});await assert.rejects(()=>service.create('a',{category:'故障排查',description:'太短',device:'手机'}),/10–2000/)})
